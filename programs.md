//practice stagging
by using git reset ,git hard and git soft
//create branches and working parallel (create 4 branches feature a,b,c,d)
//make modification under each of the branches and commit at the branch level for all the branches
//try merging feature c,and d branches .whike merging resolve the merge conflicts and accomidate the changes
//at the end merge all the branches created into the main branch
//clone a remote repository into local directory,modify the implementation and push the updated changes back to remote repository
//publish the application using github 